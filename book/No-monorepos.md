# No monorepos {#no-monorepos}

I favor separate repos.

One reason is that with a mono-repo you are not dogfooding your own packages (because you can run/build/develop everything in place).

This works especially well with [_semantic-release_]({#semantic-release}).
